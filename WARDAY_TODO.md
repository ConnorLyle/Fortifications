# War Day TODO

This is the canonical War Day backlog. Agents must follow the queue and completion rules in [`AGENTS.md`](AGENTS.md).

## Player and entity state

- [ ] Make non-player entities carry over into the War Day dimension and define which entity categories must be copied or transferred.
  - Implementation added 2026-08-04: `/warday prepare confirm` now captures transformed, persistent non-player entity templates from the actual claimed chunks, saves them in `WarDayState`, and `/warday start` creates fresh temporary clones for that match. Match end removes loaded clones, and a persisted per-match batch marker rejects stale clones when unloaded chunks later load.
  - Supported by default: saveable non-player entities, including living mobs, tamed animals, armor stands, vehicles, passengers, and compatible modded entities. Excluded: players, paintings/item frames handled by the separate decorative path, drops, XP, projectiles, displays, transient hazards, leash knots, the Ender Dragon, and the Wither.
  - Safety/configuration: `prepare.maxPreparedEntities` defaults to 256 across both copied areas; exact claim membership prevents copying entities from unclaimed holes. Entity UUIDs are regenerated per match, owner UUIDs remain intact, vehicle/passenger trees are preserved, and fence-leash positions are transformed with the arena.
  - Verification update 2026-08-06: continuous entity positions are now rotated around the anchor block center, keeping entities inside the same transformed block as the block-copy mapping instead of shifting them into an adjacent block at 90/180-degree rotations. A focused calculation checked 900 within-block positions across all four rotations. `gradle build --no-daemon --no-problems-report` succeeded; the build artifact and refreshed `MYTH MODS FOR DEREK/warday-1.0.0.jar` both have SHA-256 `05277987197345962CA438506ED73E64AD9EEAD4E34CDE7DCBD14D75B4E183EA` (109,685 bytes). No Gradle project test covered this entity verification.
  - Manual verification still required before completion: run prepare/start/end twice with representative vanilla and modded entities, all four defender rotations, a server restart during an active match, an entity in an unclaimed hole, and enough entities to exercise the configured cap. Confirm no originals are changed and no temporary duplicates survive.
  - [ ] Ensure tamed entities, especially tamed wolves, carry over with their owner, tame state, health, equipment, name, and other relevant NBT intact.
    - Source implementation preserves the serialized entity NBT while replacing only entity-instance UUIDs and copied-entity leash references. The player's owner UUID is deliberately not rewritten.
    - Manual verification still required: use a named, damaged, sitting, collar-dyed wolf plus a standing/following wolf; confirm owner, health, collar, name, pose, behavior, leash behavior, cleanup, and recreation in a second match.
- [ ] Fix paintings so they do not break when placed or copied and are present in the War Day dimension with the correct position, facing, and variant.
  - Implementation added 2026-08-04: decorative copying now transforms the painting `facing` value, entity position, attached tile position, and yaw through the same base rotation while preserving the serialized painting variant. Item frames use their separate `Facing` field and are rotated through the same path.
  - Copying is restricted to hanging entities attached inside the actual claimed chunks. Previously prepared paintings/item frames in the transformed destination claim shape are removed before recopying, preventing repeat-prepare collisions and duplicates.
  - Every clone must retain its expected direction and painting variant, pass vanilla `survives()` support/collision validation, and succeed in `addFreshEntity`; failures are counted in `/warday prepare confirm` output instead of being reported as successful copies.
  - Build verification: `gradle build --no-daemon` succeeded in `Warday-Mod/Warday` on 2026-08-04. The rebuilt `MYTH MODS FOR DEREK/warday-1.0.0.jar` has the same SHA-256 hash as the build artifact: `DB808B4055AF8AC5421FA9C01A4553C71B19F95B2DA488C6C82EA7C503CB4552`.
  - Manual verification still required before completion: test direct placement and copied 1x1, wide, tall, and large paintings on all wall directions; prepare all four defender rotations twice; verify exact variants, attachment positions, no item drops/duplicates, survival beyond 100 ticks, an unclaimed-hole exclusion, and persistence after a server restart.
- [ ] Snapshot each participant's complete pre-War-Day inventory state and restore that exact state after War Day.
  - Items gained, moved, damaged, placed, consumed, or lost during War Day must not permanently alter the player's pre-event inventory.
  - Include armor, offhand, main inventory, selected hotbar slot, and any other player inventory data used by the installed modpack.
  - Implementation added 2026-08-04: the match-start snapshot now persists each online participant before any teleport or match-issued item mutation. It captures the vanilla main inventory, armor, offhand, selected hotbar slot, Ender Chest, carried cursor stack, and the complete Curios inventory tag when Curios is installed.
  - Restoration runs for normal match end and saved-player recovery after logout, reconnect, or server restart. Legacy snapshots remain compatible, and a recovery snapshot is retained instead of discarded if optional Curios data cannot be restored safely.
  - Duplication safeguards updated 2026-08-11: players cannot interact with arena container blocks, item-handler blocks, item frames, vanilla `Container` entities such as chest boats/minecarts, or entities exposing NeoForge item handlers. The installed EnderStorage pouch (`enderstorage:ender_pouch`) is blocked for both block-targeted and ordinary right-click use throughout an active match.
  - Build verification: `gradle build --no-daemon` succeeded in `Warday-Mod/Warday` on 2026-08-04. The rebuilt `MYTH MODS FOR DEREK/warday-1.0.0.jar` matches the build artifact at SHA-256 `335E29BE22FB513A250F4C09CD6A8F9A747737F4318AE22E1F0C0D25C1FA8D21`.
  - Manual verification still required before completion: test main inventory, armor, offhand, selected slot, Ender Chest, cursor-held items, every Curios slot, nested backpack contents, gained/consumed/damaged/placed items, death/respawn, logout/reconnect, and restart. Also test Ender pouch use against air/blocks, chest boat/minecart interaction, automation deposits, and modded storage blocks/entities with modifiable and non-modifiable handlers.
- [ ] Ensure temporary team blocks and other match-issued items are absent after War Day restoration.
  - This may be resolved by exact inventory restoration, but it must be tested independently before being struck off.
  - Source implementation now restores the exact pre-match snapshot after team blocks are issued, so match-issued stacks should be removed automatically. Issued stacks also carry a `minecraft:custom_data` `WardayTeamBlock` marker identifying `defender` or `attacker`; replenishment counts only correctly marked stacks, and placement requires the configured block plus the participant's matching marker. Ordinary pre-owned wool and a stolen opposing stack are rejected.
  - Independent runtime safeguard added 2026-08-05: after restoration, Warday reserializes and compares the player's vanilla inventory, selected slot, Ender Chest, cursor stack, and Curios inventory with the saved pre-match snapshot. Any mismatch is logged, reported to the player, and leaves the recovery snapshot intact instead of accepting a partial restoration.
  - Dropped items, block/entity storage contents, item handlers, and item frames are cleared during cleanup. After players are restored, end cleanup wipes the complete bounded arena volume and discards every loaded non-player entity in the dedicated Warday dimension, clearing `Container` and mutable entity-handler inventories before discard.
  - Build verification: `gradle build --no-daemon` succeeded in `Warday-Mod/Warday` on 2026-08-05. The rebuilt `MYTH MODS FOR DEREK/warday-1.0.0.jar` matches the build artifact at SHA-256 `FF607472065408511972A5C0FE92EEA8A704A4E61BEA69FEC3D6A9DE002F553A`.
  - Manual verification still required before completion: begin with zero and pre-owned matching wool; verify only issued tagged stacks place, all four replenishment paths count only tagged stacks, opposing tagged stacks fail, and tags survive split/drop/pickup. Test death/respawn, reconnect/restart, then both match-end paths and confirm the arena is air, entity storage is empty/deleted, no non-player entities survive, and only the exact pre-match inventory returns.
- [ ] Wipe the bounded War Day arena and delete non-player entities from the Warday dimension whenever War Day ends.
  - Implementation added 2026-08-11: normal `/warday end` and victory-fanfare completion share `endActiveWarDay`; after online players are restored, it clears loaded storage, removes the nexus marker, wipes every non-air block across the full configured arena/build-height volume, clears vanilla and mutable-capability entity inventories, and discards every loaded non-player entity in the dimension. Cleanup counts are written to the server log.
  - The wipe deliberately targets the bounded dedicated arena rather than attempting to erase infinitely generated dimension chunks. Wiping loads all arena chunks first, so arena entities are available for the subsequent dimension-wide loaded-entity pass.
  - Build verification succeeded. Manual verification still required: populate blocks and entities at arena corners/all heights plus a loaded location outside the arena; test chest boats, chest/hopper minecarts, item frames, mobs, projectiles, prepared entities, `/warday end`, both victory paths, reconnect/restart, and a second prepare with no stale state.

## Match presentation and HUD

- [ ] Add a configurable victory fanfare after the nexus is broken instead of immediately teleporting everyone to the Overworld.
  - Target behavior: freeze/end combat, announce the winning team with a title card, play celebratory effects such as fireworks, wait about 30 seconds by default, and then restore/return players safely.
  - Prevent duplicate nexus-break handling, deaths, disconnects, or a server restart during the fanfare from corrupting match state or restoring players more than once.
  - Implementation added 2026-08-05: nexus destruction now atomically changes the persisted match from combat to a victory-fanfare phase instead of restoring players inside the block-break event. Defender wins caused by the match timer use the same presentation and cleanup path.
  - `match.victoryFanfareSeconds` is configurable from 0–300 seconds and defaults to 30. The saved state records the fanfare deadline, winning team, victory reason, and final actor, while older active-match saves load as normal combat matches.
  - At fanfare start, saved players enter spectator mode, pending respawns and combat trackers are cleared, title/subtitle cards announce the winner, and safe firework/celebration particles and sounds repeat around the nexus. An action-bar countdown runs until the normal exact-state restoration and arena cleanup execute.
  - Lifecycle safeguards: the phase transition rejects duplicate completion, building and additional block breaking are canceled, explosions cannot damage arena blocks, reconnecting saved players rejoin as fanfare spectators, newly joining outsiders remain outside, and a server restart resumes from the persisted game-time deadline. `/warday end` remains an immediate operator restoration override.
  - Build verification: `gradle build --no-daemon` succeeded in `Warday-Mod/Warday` on 2026-08-05. No automated test covered the fanfare behavior. The rebuilt `MYTH MODS FOR DEREK/warday-1.0.0.jar` matches the build artifact at SHA-256 `927E7463C3D2AA3679268BBCB54521CB6A40C80B3D0EF1FCE7A4A2A8D10F11A2`.
  - Manual verification still required before completion: test attacker nexus victory and defender timeout victory; durations 0 and 30; titles, countdown, sounds, and effects; a nexus break while a respawn is pending; duplicate/near-simultaneous breaks; logout/reconnect; a server restart mid-fanfare; `/warday end` during fanfare; and exact one-time restoration for online and offline players.
- [ ] Add a boss-bar match timer at the top of the screen and keep it synchronized with the authoritative match clock.
  - Implementation added 2026-08-06: a single server-owned boss bar displays `War Day - m:ss` to all players present during combat. It derives remaining time and progress from the persisted authoritative match deadline in the War Day dimension, updates once per second, and persists the match's original duration for stable progress normalization across server restarts. Legacy active saves fall back to the configured duration.
  - Lifecycle handling: the bar is created/reconstructed at match start or after restart, synchronizes players on start/login/logout, and is hidden and detached immediately on victory fanfare, `/warday end`, inactive-state recovery, a missing War Day level, or replacement of an integrated server in the same JVM.
  - Verification: boundary calculations passed for match start, sub-second countdown, the final two seconds, expiry, and post-expiry clamping. `gradle build --no-daemon --no-problems-report` succeeded on 2026-08-06. The build artifact and refreshed `MYTH MODS FOR DEREK/warday-1.0.0.jar` are both 111,385 bytes with SHA-256 `5FFBF88DC42834CF97CCB94D8F33962FF26EA383655A06D49272A27992399488`. No Gradle project test covered the boss bar.
  - Manual verification still required before completion: confirm bar placement/readability and one-second countdown during a match; correct progress at start and near expiry; login/logout/reconnect; death and respawn cooldown; server restart; defender timeout; attacker nexus victory; `/warday end`; fanfare transition; and no stale or duplicate bars before a second match.
- [ ] Add a sidebar scoreboard that lists Team 1 and Team 2 and their participating players.
  - Implementation updated 2026-08-10: Warday creates separate server-owned defender and attacker roster objectives with colored team headers and alphabetically sorted names from the persisted participant UUID sets. Each participant is sent the objective for their persisted side, enabling teammate-only health without changing scoreboard teams. Online names are preferred, cached offline profiles are supported, and an eight-character UUID prefix is the safe fallback.
  - Minecraft's 15-line sidebar limit is handled with symmetrical pages of up to six players per team; both team headers remain visible and pages rotate every five seconds. The global sidebar remains available to other systems while Warday selects the appropriate roster per participant. Cleanup returns clients to the current global objective and removes Warday's tracked objectives; the legacy single objective is migrated away if found.
  - Verification: pagination calculations covered every defender/attacker size combination from 0 through 30, never exceeded 14 rendered lines, and showed every participant exactly once per page rotation. `gradle build --no-daemon --no-problems-report` succeeded on 2026-08-06. The build artifact and refreshed `MYTH MODS FOR DEREK/warday-1.0.0.jar` are both 116,294 bytes with SHA-256 `C5AEEBC0A78E339416520390263C884BE80AE9BE3EADA53751B94392DC59CD9A`. No Gradle project test covered sidebar pagination.
  - Manual verification still required before completion: confirm layout, colors, ordering, exact membership, cached offline names, 7+ players per team and page rotation, duplicate/similar names, login/logout/reconnect, death/respawn, server restart during combat and fanfare, `/warday end`, normal victory cleanup, restoration of a pre-existing sidebar, and a second match without stale objectives or lines.
- [ ] If Minecraft scoreboard constraints allow a readable implementation, show each listed player's live health beside their name.
  - Update efficiently and handle death, respawn cooldown, disconnect, reconnect, and match cleanup.
  - Implementation updated 2026-08-10: each player line keeps its stable roster ordering score and uses a per-score fixed number format for a right-aligned status column. A participant sees health/status only for their own persisted team; opponent names remain listed with a blank status. Online health is numeric Minecraft health points such as `20/20` rather than hearts, including modded maximum health, with percentage coloring. Absorption remains excluded; teammate pending respawns show `RESP Ns`, and disconnected teammates show `OFF`.
  - The existing once-per-second sidebar synchronization now includes health/status in its change signature, reuses stable score holders instead of clearing the whole objective, and removes only stale line holders. This avoids per-tick work and full-sidebar resets while retaining page rotation, restart reconstruction, external-objective restoration, and cleanup behavior.
  - Automated verification: `WarDayRosterHealthTest` covers numeric health points, boosted/clamped/invalid health, every color threshold, offline state, and respawn countdown rounding. It is attached to Gradle `check`; the current 2026-08-11 build artifact and refreshed distributable are both 149,681 bytes with SHA-256 `86135FD12D5C885DE50EE4FE1F039B0B9BFB74EEF500AE8883D9B7FC727B208C`.
  - Manual verification still required before completion: confirm each team sees only teammate health/status, `20/20` at vanilla full health, readable columns with varied names, damage/healing, boosted maximum health, zero health, respawn countdown, disconnect/reconnect, restart during combat/fanfare, page rotation, both cleanup paths, external-sidebar coexistence/restoration, and a second match without stale values or flicker.

## Respawn experience

- [ ] During respawn cooldown, make the dead player spectate a living teammate in first-person POV when one is available.
  - Allow cycling through living teammates with spectator controls, preferably left click and right click if reliable; at minimum, provide a reliable one-direction cycle.
  - Handle the viewed teammate dying, disconnecting, or being the last living teammate, and restore the respawning player normally when the cooldown ends.
  - Implementation updated 2026-08-10: a respawning participant enters entity-camera POV for an eligible teammate selected in stable name/UUID order. Candidates must belong to the same persisted War Day team, be online and alive in the War Day dimension, not be spectators, and not have their own pending respawn. Invalid targets are replaced automatically; when no target exists, the viewer is held above team spawn and automatically attaches when one becomes available.
  - The original normal attack/use mapping was replaced because spectator mode did not reliably emit it. A synchronized cooldown flag now gates raw left/right mouse press handling while no menu is open: left requests the previous teammate and right requests the next. The server still validates direction, combat/pending-respawn/spectator state, and the complete candidate list before changing the camera.
  - Camera and client-input state are reset before normal respawn restoration, fanfare spectator assignment, exact player restoration, `/warday end`, and reconnect/restart reconstruction. Camera targets are runtime-only; persisted pending-respawn ticks and team rosters are sufficient to choose a fresh valid target after reconnect or restart.
  - Automated verification: `WarDaySpectatorCycleTest` covers teammate selection and raw LMB/RMB press mapping, including release/other-button rejection. The 2026-08-10 full build and focused checks succeeded; artifact evidence is recorded under the live-health item above.
  - Manual verification still required before completion: use at least three participants on one team and an opponent; verify automatic first-person attachment, left/right cycling and wraparound, opponent exclusion, target death/logout/dimension change/pending respawn, no living teammate followed by a teammate becoming available, viewer logout/reconnect, server restart during cooldown, cooldown completion, fanfare, `/warday end`, normal victory cleanup, ordinary spectator controls, repeated deaths, and a second match without a stuck camera or intercepted inputs.

## Arena preparation

- [ ] Rework the attacker-side arena copy strategy so substantially more surrounding terrain/chunks are brought into War Day instead of relying on terrain generated between a small attacker spawn copy and the defender base.
  - Decide whether this is a configurable radius, a connected claim expansion, or another bounded plan before implementation.
  - Keep `/warday prepare` preview, destination conflict checks, rotation/placement logic, performance limits, and repeatable destination clearing accurate for the enlarged copy area.
  - Implementation revised 2026-08-14 to match the requested corner layout: the 250x250 battlefield background now comes from a sequence-selected remote area naturally generated in the defender's source dimension, not from terrain surrounding the defender's nexus. Candidate selection requires every quart-biome sample across the arena-sized window to match the nexus biome before any source chunks are loaded. Surface bases validate each column at its generated surface height; ceiling dimensions and sufficiently underground nexuses validate at the nexus layer. The coordinates remain safely remote and use the exact bounded source window plus the existing 289-chunk guardrail.
  - The claimed defender base alone is rotated so its forward marker faces west and is fitted fully into the northeast arena corner. The attacker target is inset 16 blocks in the diagonal southwest corner. Oversized rotated claims fail before preparation; large claims that fit can reduce only the defender corner margin while the attacker retains the safe border inset.
  - Every successful preparation advances a persisted terrain-generation sequence. Preview and confirm use the same upcoming sequence, including across restart; only successful prepared-state saving consumes it. Consecutive confirmations therefore select fresh remote terrain while a failed confirmation remains safely repeatable. Search radius and deterministic remote-region attempt count are configurable and preparation fails clearly when the biome is too small or rare to supply a continuous window.
  - Repeat preparation clears the bounded arena, copies the newly selected naturally generated biome terrain, and applies the defender claim last so defender blocks and air deterministically win overlaps. Container, decorative-entity, prepared-entity, clipping, and safe-surface-spawn safeguards remain active. Match border, build/break bounds, transient cleanup, storage cleanup, and arena wipe are centered on the arena origin rather than the relocated nexus.
  - `/warday prepare` reports the upcoming fresh generation number, matched biome, generated 250x250 coverage, remote source chunks, defender corner placement/rotation, and diagonal opposite-corner attacker target. The attacker setup block remains unnecessary; Team 2 is still selected by `/warday team2 <name>`.
  - After terrain/base placement, confirm also applies the selected biome through every vertical biome cell in all arena-intersecting chunks, marks them for saving, and sends batched biome updates to players already in the War Day dimension. This keeps foliage/water tint, precipitation, ambient effects, biome reporting, and biome-driven rules consistent with the naturally generated biome blocks and features.
  - Automated verification: `WarDayAttackerTerrainPlanTest` covers corner fitting/default and reduced margins, diagonal spawn coordinates, oversized-claim rejection, preview-stable deterministic remote-source selection, different source regions across generation sequences/search attempts, exact 250x250 windows for every rotation, negative-coordinate floor division, border semantics, and the default chunk guardrail. `gradle build --no-daemon --no-problems-report --no-build-cache` succeeded with every focused check after biome-authentic regeneration and generated-surface validation on 2026-08-14; the build and refreshed distributable are both 157,196 bytes with SHA-256 `CA046F8ED9D615676FB0F0ECD4BECFDAA07343DA99886106DEBAB369DC6536FA`.
  - Manual verification still required before completion: preview/confirm all four forward-marker directions and representative positive/negative source coordinates; inspect all arena edges and the full route between corners; confirm only the defender claim is overlaid, both teams land safely, large/irregular claim behavior is clear, and prepare/search responsiveness is acceptable. For plains, desert, snowy, Nether, and one modded defender biome, verify terrain blocks/features, reported biome ID, full-arena biome readout, tint, weather, and restart persistence. Run two consecutive successful confirms and prove the terrain changes while the biome remains matched; test a deliberately small/rare biome failure and non-default search settings. Then run start, death/respawn, logout/reconnect, server restart, normal victory cleanup, and `/warday end` with the relocated nexus and origin-centered border.
- [ ] Set the match nexus hardness to 22.5 and show a definitive completion message after successful `/warday prepare confirm`.
  - Implementation added 2026-08-10: the nexus uses hardness `22.5` while retaining blast resistance `30`; the legacy attacker marker keeps its prior properties. Successful preparation now ends with a bold green message confirming the arena, automatic spawn, nexus, and entity templates are ready and directing the operator to `/warday start`. Failure paths return before this message.
  - Build verification: the full Warday build and all focused checks succeeded; the current refreshed distributable matches the artifact at 149,681 bytes with SHA-256 `86135FD12D5C885DE50EE4FE1F039B0B9BFB74EEF500AE8883D9B7FC727B208C`. Manual verification still required: compare nexus mining time with the intended tool/effects and confirm the completion line appears exactly once after success and never after a failed prepare.

## Combat rules

- [ ] Disable friendly fire between participants on the same persisted War Day team during active combat.
  - Implementation added 2026-08-10: incoming player-caused damage is canceled when attacker and victim belong to the same persisted defender or attacker participant set in the War Day dimension. The source player is resolved through the damage source, so punches, swords, and player-fired projectiles share the same rule. Opponent, outsider, and environmental damage are unchanged.
  - Automated verification: `WarDayFriendlyFireTest` covers defender/attacker teammates, opponents, and outsiders and runs under Gradle `check`; the full build passed.
  - Manual verification still required before completion: test fists, swords/axes, bows/crossbows/tridents, modded melee/projectiles, knockback/fire side effects, opponents, outsiders, death/respawn, reconnect, fanfare, match end, and a second match.

## Skills and map privacy

- [ ] Remove the combat and mining skill trees from the shipped gameplay/configuration.
  - Remove their registration and presentation cleanly; migrate or safely ignore existing player data without breaking other skill trees.
  - Implementation added 2026-08-07: the Fortifications mod now overrides the Default Skill Trees datapack config at `data/puffish_skills/puffish_skills/config.json` with a version-3 empty category list. Its optional NeoForge dependency orders Fortifications after `default_skill_trees`, so the combat and mining category files are neither registered nor presented while the independent `fortifications_classes` namespace remains untouched.
  - Existing category allocations are intentionally not deleted from Puffish Skills player data. With the categories absent they are inert and safely ignored, avoiding a destructive migration and allowing rollback if the default trees are ever deliberately restored.
  - Build/package verification: `.\gradlew.bat build --no-daemon --no-problems-report` succeeded in `Fortifications Mod/Fortifications Mod` on 2026-08-07. Static JAR inspection confirmed the empty categories resource and optional `default_skill_trees` `AFTER` ordering are packaged. The build artifact and refreshed `MYTH MODS FOR DEREK/fortifications-1.0.0.jar` are both 78,638 bytes with SHA-256 `9E7137E7ABA02BD5009005D3A2E6056274A4F378DE3700FD092F96116D4525EB`.
  - Manual verification still required before completion: launch with Puffish Skills 0.18.3, Default Skill Trees 1.1, and existing player data; confirm only the Classes tree is shown, combat/mining rewards are inactive, no load warnings occur, `/reload` does not restore them, new players behave identically, and removing the optional Default Skill Trees JAR does not break Fortifications startup.
- [ ] Investigate and implement the strongest feasible JourneyMap integration/configuration that hides non-teammate player icons while retaining teammate icons.
  - Confirm what can be enforced server-side with the installed JourneyMap version/API. If exact team filtering is impossible, document the limitation and use the safest server-enforceable fallback.
  - Investigation and implementation added 2026-08-07 for installed JourneyMap 6.0.3/API 2.0.0: JourneyMap posts a server-side `PlayerRadarUpdateEvent` for each receiver/remote-player pair before sending radar visibility. Warday subscribes through `ServerEventRegistry.PLAYER_RADAR_UPDATE_EVENT` when JourneyMap is installed and keeps a target visible only when both players resolve to the same FTB Team ID.
  - The filter is global rather than War-Day-only and applies equally to operators. It preserves any stricter JourneyMap decision (dimension, invisibility, spectator, underground, or server options) and only removes additional visibility. Missing FTB manager/team state fails closed. JourneyMap remains optional and Warday has no classloading path into its API when the mod is absent.
  - Automated verification: `WarDayTeamVisibilityTest` covers same team, different teams, JourneyMap already-hidden targets, either missing team, and unavailable team state. All four focused checks run under Gradle `check`; `gradle build --no-daemon --no-problems-report` succeeded on 2026-08-07. Packaged metadata statically confirms the optional JourneyMap 6.0.3–6.0.x dependency. The build artifact and refreshed `MYTH MODS FOR DEREK/warday-1.0.0.jar` are both 139,208 bytes with SHA-256 `79A9AB4ED1BE5D313940A9CA4AB5826266F8948EF18C2AC381ABA30B0C7CA651`.
  - Manual verification still required before completion: use two players on one FTB team, an opponent, and an unteamed/personal-team player; verify fullscreen/minimap icons, operators, same/different dimensions, invisibility/sneaking/spectator states, team join/leave/switch without restart, login/reconnect, `/reload`, server restart, JourneyMap server-option changes, and Warday startup with JourneyMap removed.

## Anti-grief rule

- [ ] Add a configurable rapid-block-breaking punishment.
  - Default threshold: one player breaks 15 blocks within a rolling 30-second window.
  - Default punishment: apply a 25% mining-speed penalty and Glowing for 60 seconds.
  - Define whether the rule applies only during an active War Day and which modes, dimensions, blocks, operators, or protected/admin actions are exempt.
  - Use per-player rolling-window tracking, avoid counting cancelled break events, clean up expired tracking state, and avoid stacking the penalty incorrectly when retriggered.
  - Provide clear player feedback when the threshold triggers and verify cleanup at match end, logout, and server restart as appropriate.
  - Implementation reconciled 2026-08-07: `match.digLimitBlocks` now defaults to 15 and triggers on the 15th successful break within the inclusive rolling 30-second window. Duration remains configurable at 60 seconds by default, and new `match.digPenaltyPercent` defaults to an exact 25% reduction.
  - Counting runs at lowest event priority after cancellation. It applies only during active combat to persisted participants in Survival, in the configured War Day dimension and border. Creative/spectator players, outsiders, canceled/protected/out-of-bounds actions, nexus victory, and Warday setup markers are exempt.
  - Punishment uses one named transient `BLOCK_BREAK_SPEED` `ADD_MULTIPLIED_TOTAL` modifier plus Glowing. Re-triggers now advance a match-scoped cumulative tier: 25%, 45%, 55%, then 60% reduction (40% remaining mining speed), capped at tier four. Updating one additive modifier gives exact cumulative percentages; every strike refreshes the unchanged duration. Strike tier survives normal effect expiry and reconnect within the running match, then clears at fanfare, match end, inactive recovery, or server stop.
  - Automated verification: `WarDayRapidBreakRuleTest` covers breaks 1–14, the 15th-break trigger, history reset/retrigger, rolling-window boundaries, exact modifier conversion, and all four cumulative tiers/cap. The 2026-08-10 full build and focused checks succeeded; artifact evidence is recorded under the live-health item above.
  - Manual verification still required before completion: trigger all four tiers and measure 75/55/45/40% remaining speed; retrigger before and after expiry; verify duration refresh, feedback, Glowing, exemptions, logout/reconnect tier continuity, death/respawn, fanfare, both cleanup paths, server restart behavior, pre-existing Glowing, non-default first-tier configuration, and a second match without stale state.
